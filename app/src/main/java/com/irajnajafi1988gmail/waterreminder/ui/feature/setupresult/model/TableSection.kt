package com.irajnajafi1988gmail.waterreminder.ui.feature.setupresult.model

import com.irajnajafi1988gmail.waterreminder.R

enum class TableSection(val label: String, val icon: Int) {
    HOME(label = "Home", icon = R.drawable.drop_image),
    HISTORY(label = "History", icon = R.drawable.history),
    SETTINGS(label = "Settings", icon = R.drawable.settings)
}